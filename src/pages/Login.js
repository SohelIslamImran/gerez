import React from 'react';
import LoginModal from '../components/Login/LoginModal';

const Login = () => {
    return (
        <div>
            <LoginModal />
        </div>
    );
};

export default Login;